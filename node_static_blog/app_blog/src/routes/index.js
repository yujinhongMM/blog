import React from "react";
import { Router, Route, Link } from 'react-router-dom';

const ROUTE_PREFIX = '/yujinhong';
function AppRouter () {
    return <Route basename={`${ROUTE_PREFIX}`}>

    </Route>
}

export default AppRouter;